{
    "name": "nextjs-monorepo-template",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
      "apps/*",
      "packages/*",
      "r-packages/*"
    ],
    "devDependencies": {
      "turbo": "^1.10.0",
      "typescript": "^5.3.0",
      "eslint": "^8.0.0",
      "prettier": "^3.0.0"
    },
    "scripts": {
      "dev": "turbo run dev",
      "build": "turbo run build",
      "lint": "turbo run lint"
    }
  }